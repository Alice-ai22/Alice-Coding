# verification-mcp-server

Local MCP server for selecting verification commands from `.project-ops/verification.json` and project metadata.

Tools:

- `verification_select`: choose relevant verification commands for a task.
- `verification_matrix`: read the project verification matrix.
- `verification_record`: write a verification result under `.agent-runs/<run>/verification.md` or `.project-ops/verification-history.md`.
